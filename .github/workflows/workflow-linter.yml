---
name: Workflow Linter

on:
  pull_request:
    paths:
      - .github/workflows/**

jobs:
  call-workflow:
    if: false
    uses: bitwarden/gh-actions/.github/workflows/workflow-linter.yml@master
